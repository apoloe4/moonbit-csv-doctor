{
  "name": "moonbit-csv-doctor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first CSV quality checks powered by MoonBit",
  "engines": {"node": ">=22"},
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "start": "node scripts/serve.mjs",
    "demo": "node cli.mjs examples/orders.csv --rules examples/rules.json --html reports/orders.html --json reports/orders.json"
  }
}
